networks:
  npm-network:
    external: true
services:
  web:
    container_name: louvre_security
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    networks:
      - npm-network